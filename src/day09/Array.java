package day09;

import java.util.Arrays;
import java.util.List;

public class Array {
    public static void main(String[] args) {


/*
    String input = "Java is easy";
    String arr[] = input.split("");//input u arr ye cevirdim

    List<String> str=new ArrayList<>(Arrays.asList(arr));//arr yi liste cevirdim
        System.out.println(str);

    List<String> enCokTekrarEdenKarakter=new ArrayList<>();//bos bir en cok tekrar eden listi olusturdum
    int maxTekrar=0;
    int tekrar=0;
        for (int i = 0; i < str.size() ; i++) {
        for (int j = i+1; j < str.size(); j++) {
            if (str.get(i).equalsIgnoreCase(str.get(j))){
                tekrar++;
            }
        }
        if (tekrar > maxTekrar) {
            maxTekrar= tekrar;
            enCokTekrarEdenKarakter.add(str.get(i));//en cok tekrar eden karakteri yeni liste ekledim
        } else if (tekrar == maxTekrar)
            // enCokTekrarEdenKarakter.add(str.get(i)) ;*** burasını cok denedim ama bulamadım yoruma aldım sonucu baska cıkarıyor
            tekrar = 0;
    }
        System.out.println(enCokTekrarEdenKarakter);
*/

    }

}
